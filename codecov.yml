coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 2%
    patch:
      default:
        target: 75%

flags:
  unittests:
    paths:
      - src/

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
